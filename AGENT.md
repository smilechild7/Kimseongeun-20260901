# 프로젝트 구현 운영 규칙

이 문서는 `Levit Problem Solver — AI Shopping Agent`를 구현하는 모든 작업에서 따라야 하는 협업·의사결정·검증·기록 규칙이다.

## 1. 기준 문서

- 제품 범위와 아키텍처의 기준은 `docs/levit_problem_solver_FINAL_PLAN.md`다.
- 현재 진행 단계와 작업 이력의 기준은 `docs/IMPLEMENTATION_STATUS.md`다.
- 작업 방식과 의사결정 절차의 기준은 이 문서다.
- 사용자의 최신 명시적 지시가 문서보다 우선한다.
- 기준 문서 사이에 충돌이 있으면 임의로 해석하지 않고 사용자에게 결정받는다.

## 2. 작업 시작 전 필수 확인

모든 구현 작업을 시작하기 전에 다음을 수행한다.

1. `AGENT.md`를 확인한다.
2. `docs/levit_problem_solver_FINAL_PLAN.md`에서 현재 작업의 범위와 DoD를 확인한다.
3. `docs/IMPLEMENTATION_STATUS.md`에서 현재 단계, 완료 항목, blocker와 다음 작업을 확인한다.
4. 기존 변경사항과 현재 repository 상태를 확인하고 사용자의 작업을 보존한다.
5. 구현 전에 사용자 결정이 필요한 트레이드오프가 있는지 식별한다.

## 3. 사용자 결정이 필요한 사항

구현 방법에 둘 이상의 의미 있는 선택지가 있고 선택에 따라 제품 동작, 범위, 일정, 유지보수성 또는 비용이 달라지면 반드시 구현 전에 사용자에게 묻는다.

대표적인 대상:

- 확정 계획에서 벗어나는 아키텍처 변경
- 새로운 dependency, framework 또는 외부 서비스 도입
- 데이터 source 및 대상 쇼핑몰 선택
- crawler 수집 범위, fallback 또는 차단 대응 방식
- schema, API contract 또는 데이터 보존 정책 변경
- hard/soft filter 기준 및 retrieval 동작 변경
- AI prompt 정책, model 설정, tool 구조 또는 hallucination 방지 방식 변경
- 개인정보, 보안, 비용 또는 외부 API 호출에 영향을 주는 결정
- UI 정보 우선순위나 핵심 interaction 변경
- 테스트 범위 축소 또는 Definition of Done 완화
- 배포 방식, 운영 설정 또는 외부 시스템 상태 변경
- 구현 일정상 기능을 축소·연기·대체하는 결정

다음은 별도 질문 없이 진행할 수 있다.

- 이미 최종 계획이나 사용자의 이전 답변으로 확정된 선택
- 동작과 계약을 바꾸지 않는 기계적 구현 세부사항
- formatter 적용, 이름 정리, 명백한 오류 수정 같은 저위험·가역 작업
- 확정된 DoD를 검증하기 위한 read-only 확인과 테스트

판단이 애매하면 트레이드오프로 간주하고 묻는다.

## 4. 결정 요청 방식

한 번의 결정 요청에는 다음을 간결하게 포함한다.

```text
[결정 필요] 결정할 주제

배경: 왜 지금 결정해야 하는가

선택지 A — 추천
- 장점
- 단점/영향

선택지 B
- 장점
- 단점/영향

추천: A를 추천하는 구체적인 이유
질문: 어느 선택으로 진행할까요?
```

규칙:

- 추천안을 항상 명시하되 사용자가 선택하기 전에는 적용하지 않는다.
- 선택지를 불필요하게 늘리지 않고 실제 가능한 안만 제시한다.
- 비용, 일정, 품질, 복잡도와 향후 변경 난이도를 숨기지 않는다.
- 한 결정이 다른 결정을 좌우하면 선행 결정부터 묻는다.
- 답변을 기다리는 동안 해당 선택에 의존하지 않는 안전한 작업만 진행한다.
- 사용자의 결정을 `docs/IMPLEMENTATION_STATUS.md`에 근거와 함께 기록한다.

## 5. 구현 원칙

- 현재 Phase 범위와 DoD를 우선하고 다음 Phase 기능을 무단으로 앞당기지 않는다.
- Non-goals에 포함된 기능을 추가하지 않는다.
- 실제 상품·가격·이미지·URL·색상·사이즈·리뷰 정보는 crawler/DB를 사실의 원천으로 사용한다.
- LLM은 해석·비교·요약을 담당하며 없는 factual data를 생성하지 않는다.
- 리뷰 근거가 없으면 `unknown`, 의견이 갈리면 `mixed`로 처리한다.
- 사용자의 기존 파일과 변경사항을 보존한다.
- secret, 개인정보, reviewer 식별정보와 실제 사용자 대화를 repository나 로그에 기록하지 않는다.
- 필요한 최소 복잡도로 구현하며, 복잡도를 추가해야 하면 먼저 결정받는다.
- 외부 상태를 변경하거나 복구하기 어려운 작업은 정확한 대상과 권한을 확인한다.

## 6. 상태 로그 관리

모든 구현 작업에서 `docs/IMPLEMENTATION_STATUS.md`를 반드시 갱신한다.

작업 시작 시:

- 현재 Phase와 작업 항목을 `진행 중`으로 표시
- 목표와 예상 검증 항목 기록
- 이미 확인된 blocker 기록

사용자 결정 시:

- 결정 ID 부여
- 검토한 선택지
- 사용자 선택
- 선택 이유와 영향
- 결정 날짜 기록

작업 완료 시:

- 완료 체크리스트 갱신
- 실제 검증 명령과 결과 요약
- 변경된 주요 파일 기록
- 발견된 제한사항과 blocker 기록
- 다음 작업을 구체적으로 기록
- Phase DoD를 모두 만족했을 때만 Phase를 `완료`로 표시

작업이 실패하거나 중단되어도 실패 원인과 재개 지점을 기록한다.

## 7. 사용자 작업 사전·사후 안내

Render 배포, 계정 로그인, 인증 복구, API key 발급·입력, 외부 서비스 연결, 수동 UI 확인처럼 사용자가 직접 해야 하는 작업은 Phase 실행 전후에 반드시 안내한다.

Phase 시작 전:

- 해당 Phase에서 사용자 작업이 필요한지 먼저 확인한다.
- 필요하다면 작업 내용, 필요한 시점, 준비할 계정·권한·값과 예상 소요를 구현 전에 안내한다.
- 사용자 작업이 완료되어야 구현을 시작할 수 있는지, 구현 후에 수행해도 되는지 구분한다.
- 구현 중간에 사용자 작업이 필요할 가능성이 있으면 미리 예고한다.

Phase 종료 전후:

- 사용자가 실제로 수행해야 할 작업을 빠짐없이 체크리스트로 제공한다.
- 각 작업의 목적, 실행 위치, 입력값, 정상 결과와 실패 시 확인할 내용을 포함한다.
- 사용자 검증이 Phase DoD에 포함되면 검증 결과를 받기 전까지 Phase를 완료로 표시하지 않는다.
- 사용자 작업이 없으면 `사용자 수동 작업 없음`이라고 명시한다.
- 수행 결과를 전달받으면 상태 로그의 체크리스트, 검증 기록과 blocker를 즉시 갱신한다.

사용자가 뒤늦게 필수 작업을 알게 되는 일이 없도록, 외부 계정이나 권한이 필요한 항목은 가능한 가장 이른 시점에 알린다.

## 8. 검증 원칙

- 변경 위험에 비례해 가장 작은 관련 검증부터 전체 build까지 수행한다.
- deterministic logic은 자동 테스트로 검증한다.
- 실제 AI 품질과 crawler 결과는 계획된 수동 evaluation을 함께 수행한다.
- 테스트를 실행하지 못했으면 성공한 것처럼 표현하지 않고 이유를 기록한다.
- 테스트가 0개인 실행은 자동 테스트 통과로 간주하지 않는다.
- build 성공과 기능 정상 동작을 구분해서 기록한다.
- 외부 배포는 실제 URL과 health endpoint를 확인해야 완료다.

## 9. Phase 종료 조건

- 계획서의 해당 Phase DoD를 모두 충족한다.
- 관련 build/test/manual check가 완료된다.
- 남은 blocker가 없거나 사용자가 명시적으로 다음 단계 진행을 승인한다.
- 상태 로그가 실제 repository 상태와 일치한다.
- 사용자에게 완료 결과, 검증 내용과 다음 단계를 보고한다.

## 10. Git 및 배포

- 의미 있는 문제 해결 단계가 보이는 단위로 변경을 구성한다.
- commit 또는 push 여부가 명시되지 않았으면 사용자에게 확인한다.
- 사용자의 미완료 변경을 임의로 commit하지 않는다.
- `.env`, API key, local DB와 build output을 commit하지 않는다.
- 배포 설정 변경과 실제 배포는 구분해 기록한다.
- Render 외부 URL과 `/api/health`를 확인하기 전에는 배포 완료로 표시하지 않는다.

## 11. 작업 완료 보고

최종 보고에는 최소한 다음을 포함한다.

- 완료한 결과
- 중요한 구현 결정과 사용자 승인 내용
- 수행한 검증과 결과
- 남은 blocker 또는 제한사항
- 상태 로그 위치
- 사용자가 해야 할 작업과 수동 검증
- 다음 작업
